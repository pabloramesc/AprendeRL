# v0.1 — Core API and vanilla DQN (draft)

This draft release marks the repository's current status as v0.1 according to ROADMAP.md.

Scope
- Core API and vanilla DQN.
- Gymnasium interaction, typed configuration, PyTorch networks, replay storage, callbacks, evaluation, logging, checkpointing, and a basic DQN implementation.

Notes
- This is a draft release file created by an assistant. To publish an actual Git tag and GitHub Release, follow the commands below or use the GitHub Web UI.

Suggested changelog (high level)
- Add core algorithm interfaces and training loops (BaseAlgorithm, OffPolicyAlgorithm, OnPolicyAlgorithm).
- Add a compact NumPy replay buffer (ReplayBuffer) ready for DQN-style algorithms.
- Logging, callbacks, and training utilities for reproducible experiments.

How to tag locally and publish on GitHub (recommended)

Using git + GitHub CLI (gh):

1. Create an annotated tag and push it:

   git tag -a v0.1 -m "v0.1: Core API and vanilla DQN"
   git push origin v0.1

2. Create a GitHub release using the gh CLI (attaches this file as the release notes):

   gh release create v0.1 --title "v0.1" --notes-file RELEASES/v0.1.md

Using git + GitHub web UI:

1. Push the tag:

   git tag -a v0.1 -m "v0.1: Core API and vanilla DQN"
   git push origin v0.1

2. Open the repository Releases page and click "Draft a new release", choose tag "v0.1", paste the notes from this file, and publish.

If you want, I can:
- Create the annotated tag and release on your behalf if you provide a GitHub personal access token with repo permissions, or
- Open a PR that adds this draft release file instead of committing directly to the default branch.

